// Redirect to the page set in `setLink()` after 3000ms
var redirect = setInterval(function () {setLink()}, 3000);

// Set link to redirect to
function setLink() {
    window.location="https://bit.ly/3szwQv1"
}